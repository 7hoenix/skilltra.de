<script type="text/javascript">
    mixpanel.track("User Created");
</script>


    <%= form_for(@user) do |f| %>
        <% if @user.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

              <ul>
                <% @user.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
              </ul>
            </div>
        <% end %>
        <h1 class="align-left"> Get Verified </h1>
        <h1 class="align-center">Who are you?</h1>

        <!--<div class="btn-group form-group" data-toggle="buttons-radio">-->
        <!--<button type="button" class="btn btn-primary">Hacker</button>-->
        <!--<button type="button" class="btn btn-primary">Hipster</button>-->
        <!--<button type="button" class="btn btn-primary">Hustler</button>-->
        <!--</div>-->

        <!--<div class="btn-group" data-toggle="buttons-radio">-->

        <!--</div>-->



        <div class="form-group">

          <%= f.fields_for :skills, @skills do |user_skill| %>

              <%= user_skill.label user_skill.name %>
              <%= @skills.order('name ASC').all.collect { |c| [c.name, c.id] } %>
              <%= user_skill.check_box :name %>
          <% end %>


        </div>


        <h1 class="align-center">Give a few examples of what you would be willing to trade.</h1>
        <div class="form-group">
          <%= f.text_field :bio, class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit 'Complete Profile', class: "btn btn-primary btn-lg" %>
        </div>



    <% end %>
<!-- Header -->

<!-- /.container -->

<%= form_for @user do |f| %>
    <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this account from being saved:</h2>

          <ul>
            <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>



<div class="container">

  <div class="row">
    <div class="col-lg-12">
      <div class="intro-message">
        <h1>Instructions</h1>
        <h3>Since people in here are trading real skills we need to make sure that you are able to trade something of value to everyone else.</h3>
        <p>You are going to create a survey to send out to a small group of friends that can vouch for you (don't worry we won't be emailing anyone for you).</p>
        <p>Before that though you can see the exact questions that your friends/colleagues will be answering.</p>
        <hr class="intro-divider">
      </div>
    </div>
  </div>

</div>


    <div class="form-group">

        <h3 class="align-center">What skill set would you be excited to receive feedback on? </h3>
        <%= f.label :skill_set, class: "heading" %><br>
        <%= f.select :skill_set, [['Programming','Programming'],['Marketing','Marketing'],['Design','Design'],['Business development','Business development']] %>

      <%= f.hidden_field :reviewed_id, :value => @user.id %>
      <%= f.hidden_field :user_id, :value => @user.id %>

      <%= f.submit 'get started', class: "btn btn-primary btn-lg" %>
    </div>

<% end %>
